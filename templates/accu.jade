extends _framework


block head

block body
  != page.html
  each post in widx.embed(page.parent)
    if (post != page)
      .embed-article
        .lmeta.date
          = post.date
        .head
          h2.title
            a(href=post.url)
              = post.title
        span.text
          !{post.html}
