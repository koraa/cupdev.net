extends _framework

block body
  .article-wrap
    .article.one-article
      h2.title
        = page.title
      span.text
        != page.html

      mixin metadata(page.metadata)

      include comments/comments.jade
