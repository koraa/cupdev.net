extends _framework

block body
  .article-wrap
    .article.one-article
      h2.title
        = page.title
      span.text
        != page.html

      +metadata(page.metadata)

      include comments/comments.pug
